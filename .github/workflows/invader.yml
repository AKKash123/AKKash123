name: Generate Space Invaders

on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  generate:
    runs-on: ubuntu-latest

    steps:
      - name: Generate Space Invaders
        uses: VARDHAMANPATEL23/git-invader@main
        with:
          github_user_name: AKKash123
          github_token: ${{ secrets.GITHUB_TOKEN }}
